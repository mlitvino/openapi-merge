{
  "name": "openapi-merge",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm --filter openapi-merge lint",
    "test": "pnpm --filter openapi-merge test"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "globals": "^17.6.0"
  }
}
